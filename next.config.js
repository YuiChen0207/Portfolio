/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  outDir: "build",
};

module.exports = {
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
